How to track the maxTxnId in the manifest.
The newest sstable file will always have the maxTxnId among sstable files. It could be the compacted file or the sstable written file.

## UTs
Ensure that the UTs capture both cases after restarting the database: 
1. Don't flush memtable to sstable
2. Flush memtable to sstable 

I need to add better tests which properly test repeatable read. When both are added, then tests should be present for both.