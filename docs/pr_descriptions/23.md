## Plan

1. We now need to propagate version to sstable and use that for Put and then for Get and Prefix Scan.
2. During Get and Prefix Scan, always pick the latest version only. In the current version, it would be straightforward as the first value itself would be the latest version.
3. Update the Iterate function (wip)

## Notes For Upcoming PR
1. We need to check following: 
    If currTxnId > oldestActiveTxnId --> keep it after compaction
    Else keep the newest txn Id per key --> why do we need to keep older than oldest active txn id?
        Because oldest also needs to see a consistent snapshot.